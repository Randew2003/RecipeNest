import { StyleSheet } from "react-native";
import { COLORS } from "../../constants/colors";

export const cookingStyles = StyleSheet.create({
  container: { flex: 1, backgroundColor: COLORS.background },
  header: {
    flexDirection: "row",
    alignItems: "center",
    paddingHorizontal: 18,
    paddingTop: 12,
    paddingBottom: 12,
    backgroundColor: COLORS.background,
    borderBottomWidth: 1,
    borderBottomColor: COLORS.border,
  },
  backButton: {
    width: 44,
    height: 44,
    borderRadius: 15,
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  headerCopy: { flex: 1, marginLeft: 12 },
  eyebrow: { color: COLORS.primary, fontSize: 10, fontWeight: "900", letterSpacing: 1.2 },
  headerTitle: { marginTop: 3, color: COLORS.text, fontSize: 15.5, fontWeight: "900" },
  headerProgressPill: {
    minWidth: 44,
    height: 30,
    alignItems: "center",
    justifyContent: "center",
    marginLeft: 9,
    paddingHorizontal: 10,
    borderRadius: 11,
    backgroundColor: COLORS.primarySoft,
  },
  headerProgressText: { color: COLORS.primary, fontSize: 11, fontWeight: "900" },
  content: { flexGrow: 1, paddingHorizontal: 18, paddingBottom: 35 },
  image: { width: "100%", height: 205, borderRadius: 24, backgroundColor: COLORS.border },
  recipeTitle: { marginTop: 18, color: COLORS.text, fontSize: 25, lineHeight: 31, fontWeight: "900" },
  progressHeader: { marginTop: 18, flexDirection: "row", alignItems: "center", justifyContent: "space-between" },
  progressLabel: { color: COLORS.textLight, fontSize: 12, fontWeight: "800" },
  progressValue: { color: COLORS.primary, fontSize: 12, fontWeight: "900" },
  progressTrack: { height: 8, marginTop: 9, borderRadius: 5, backgroundColor: COLORS.border, overflow: "hidden" },
  progressFill: { height: "100%", borderRadius: 5, backgroundColor: COLORS.primary },
  stepCard: {
    minHeight: 235,
    marginTop: 20,
    padding: 21,
    borderRadius: 24,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
    justifyContent: "space-between",
  },
  stepBadge: {
    alignSelf: "flex-start",
    paddingHorizontal: 11,
    paddingVertical: 6,
    borderRadius: 13,
    backgroundColor: COLORS.primarySoft,
  },
  stepBadgeText: { color: COLORS.primary, fontSize: 11, fontWeight: "900" },
  stepText: { marginTop: 17, color: COLORS.text, fontSize: 20, lineHeight: 31, fontWeight: "700" },
  tipRow: { marginTop: 18, flexDirection: "row", alignItems: "center" },
  tipText: { flex: 1, marginLeft: 8, color: COLORS.textLight, fontSize: 11, lineHeight: 16 },
  timerCard: {
    marginTop: 17,
    padding: 17,
    borderRadius: 21,
    backgroundColor: COLORS.primaryDark,
  },
  timerHeader: { flexDirection: "row", alignItems: "center" },
  timerTitle: { flex: 1, marginLeft: 9, color: COLORS.white, fontSize: 14, fontWeight: "900" },
  timerValue: { marginTop: 15, color: COLORS.white, fontSize: 37, fontWeight: "900", letterSpacing: 1.5, textAlign: "center" },
  timerButtons: { flexDirection: "row", marginTop: 15, marginHorizontal: -4 },
  timerButton: {
    flex: 1,
    marginHorizontal: 4,
    minHeight: 39,
    borderRadius: 13,
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "rgba(255,255,255,0.14)",
  },
  timerButtonActive: { backgroundColor: COLORS.white },
  timerButtonText: { color: COLORS.white, fontSize: 11, fontWeight: "900" },
  timerButtonTextActive: { color: COLORS.primaryDark },
  navigation: { flexDirection: "row", marginTop: 20, marginHorizontal: -5 },
  navigationButton: {
    flex: 1,
    minHeight: 54,
    marginHorizontal: 5,
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 17,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  navigationPrimary: { backgroundColor: COLORS.primary, borderColor: COLORS.primary },
  navigationDisabled: { opacity: 0.45 },
  navigationText: { marginHorizontal: 7, color: COLORS.text, fontSize: 13, fontWeight: "900" },
  navigationPrimaryText: { color: COLORS.white },
  completedCard: {
    marginTop: 20,
    alignItems: "center",
    padding: 28,
    borderRadius: 24,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  completedIcon: {
    width: 72,
    height: 72,
    borderRadius: 24,
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: COLORS.successSoft,
  },
  completedTitle: { marginTop: 16, color: COLORS.text, fontSize: 24, fontWeight: "900" },
  completedText: { marginTop: 8, color: COLORS.textLight, fontSize: 13, lineHeight: 20, textAlign: "center" },
});
