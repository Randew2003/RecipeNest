import { StyleSheet } from "react-native";
import { COLORS } from "../../constants/colors";

export const featureStyles = StyleSheet.create({
  container: { flex: 1, backgroundColor: COLORS.background },
  content: { paddingHorizontal: 18, paddingTop: 20, paddingBottom: 34 },
  headerFlush: { paddingHorizontal: 0, paddingTop: 0 },
  sectionHeader: {
    marginTop: 25,
    marginBottom: 12,
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
  },
  sectionTitle: { color: COLORS.text, fontSize: 18, fontWeight: "800" },
  sectionHint: { color: COLORS.textLight, fontSize: 11, fontWeight: "600" },
  dangerText: { color: COLORS.danger },
  infoCard: {
    flexDirection: "row",
    alignItems: "flex-start",
    marginTop: 16,
    padding: 13,
    borderRadius: 16,
    backgroundColor: COLORS.primarySoft,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  infoIcon: {
    width: 32,
    height: 32,
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 11,
    backgroundColor: COLORS.card,
  },
  infoText: {
    flex: 1,
    marginLeft: 10,
    color: COLORS.text,
    fontSize: 12,
    lineHeight: 18,
    fontWeight: "600",
  },

  ingredientInputArea: { position: "relative", zIndex: 5 },
  inputRow: { flexDirection: "row", alignItems: "center" },
  inputWrap: {
    flex: 1,
    minHeight: 50,
    flexDirection: "row",
    alignItems: "center",
    paddingHorizontal: 14,
    borderRadius: 16,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  input: { flex: 1, marginLeft: 8, color: COLORS.text, fontSize: 14 },
  addButton: {
    width: 50,
    height: 50,
    marginLeft: 9,
    borderRadius: 16,
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: COLORS.primary,
  },
  suggestionsCard: {
    position: "absolute",
    top: 57,
    left: 0,
    right: 59,
    zIndex: 20,
    overflow: "hidden",
    borderRadius: 16,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
    elevation: 8,
    shadowColor: COLORS.shadow,
    shadowOpacity: 0.15,
    shadowRadius: 12,
    shadowOffset: { width: 0, height: 5 },
  },
  suggestionRow: {
    minHeight: 50,
    flexDirection: "row",
    alignItems: "center",
    paddingHorizontal: 12,
    borderBottomWidth: 1,
    borderBottomColor: COLORS.border,
  },
  suggestionRowLast: { borderBottomWidth: 0 },
  suggestionIcon: {
    width: 31,
    height: 31,
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 10,
    backgroundColor: COLORS.primarySoft,
  },
  suggestionText: {
    flex: 1,
    marginHorizontal: 10,
    color: COLORS.text,
    fontSize: 13,
    fontWeight: "700",
  },
  catalogError: {
    minHeight: 46,
    flexDirection: "row",
    alignItems: "center",
    marginTop: 10,
    paddingHorizontal: 12,
    borderRadius: 14,
    backgroundColor: COLORS.warningSoft,
  },
  catalogErrorText: {
    flex: 1,
    marginLeft: 8,
    color: COLORS.warning,
    fontSize: 11.5,
    lineHeight: 16,
    fontWeight: "600",
  },
  catalogRetryText: { marginLeft: 8, color: COLORS.warning, fontSize: 12, fontWeight: "800" },
  chips: { flexDirection: "row", flexWrap: "wrap", marginHorizontal: -4 },
  chip: {
    flexDirection: "row",
    alignItems: "center",
    margin: 4,
    paddingHorizontal: 12,
    paddingVertical: 9,
    borderRadius: 15,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  chipSelected: { backgroundColor: COLORS.primarySoft, borderColor: COLORS.primary },
  chipText: { color: COLORS.text, fontSize: 12, fontWeight: "600" },
  chipTextWithIcon: { marginLeft: 5 },
  chipTextSelected: { color: COLORS.primary },
  chipRemove: { marginLeft: 6 },
  primaryButton: {
    minHeight: 52,
    marginTop: 22,
    paddingHorizontal: 18,
    borderRadius: 16,
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: COLORS.primary,
  },
  primaryButtonDisabled: { opacity: 0.5 },
  primaryButtonText: { marginLeft: 8, color: COLORS.white, fontSize: 14, fontWeight: "700" },
  secondaryButton: {
    minHeight: 50,
    marginTop: 12,
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 16,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.primary,
  },
  secondaryButtonText: { marginLeft: 8, color: COLORS.primary, fontSize: 13, fontWeight: "700" },
  inlineMessage: {
    marginTop: 15,
    padding: 13,
    borderRadius: 13,
    color: COLORS.danger,
    backgroundColor: COLORS.dangerSoft,
    fontSize: 13,
    lineHeight: 19,
  },
  emptyCard: {
    marginTop: 14,
    alignItems: "center",
    padding: 24,
    borderRadius: 19,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  emptyTitle: { marginTop: 11, color: COLORS.text, fontSize: 17, fontWeight: "800" },
  emptyText: { marginTop: 7, color: COLORS.textLight, fontSize: 13, lineHeight: 19, textAlign: "center" },

  recommendationCard: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 12,
    padding: 10,
    borderRadius: 18,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  recommendationImage: { width: 88, height: 88, borderRadius: 14, backgroundColor: COLORS.border },
  recommendationCopy: { flex: 1, paddingHorizontal: 12, paddingVertical: 3 },
  recommendationTitle: { color: COLORS.text, fontSize: 15, lineHeight: 20, fontWeight: "700" },
  recommendationMeta: { marginTop: 6, color: COLORS.textLight, fontSize: 11, fontWeight: "600" },
  recommendationMissing: {
    marginTop: 4,
    color: COLORS.warning,
    fontSize: 10.5,
    fontWeight: "600",
  },
  matchBadge: {
    alignSelf: "flex-start",
    marginTop: 8,
    paddingHorizontal: 9,
    paddingVertical: 5,
    borderRadius: 11,
    backgroundColor: COLORS.successSoft,
  },
  matchText: { color: COLORS.success, fontSize: 10, fontWeight: "700" },

  planSummary: {
    flexDirection: "row",
    alignItems: "center",
    marginTop: 20,
    padding: 14,
    borderRadius: 16,
    backgroundColor: COLORS.primarySoft,
  },
  planSummaryText: { flex: 1, marginLeft: 9, color: COLORS.text, fontSize: 12, lineHeight: 18, fontWeight: "600" },
  dayTabs: { marginTop: 20, marginHorizontal: -18, paddingHorizontal: 18 },
  dayChip: {
    marginRight: 8,
    paddingHorizontal: 15,
    paddingVertical: 9,
    borderRadius: 14,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  dayChipSelected: { backgroundColor: COLORS.primary, borderColor: COLORS.primary },
  dayChipText: { color: COLORS.textLight, fontSize: 12, fontWeight: "700" },
  dayChipTextSelected: { color: COLORS.white },
  mealSlot: {
    marginBottom: 12,
    padding: 14,
    borderRadius: 18,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  mealSlotHeader: { flexDirection: "row", alignItems: "center", marginBottom: 10 },
  mealTypeIcon: {
    width: 34,
    height: 34,
    borderRadius: 12,
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: COLORS.primarySoft,
  },
  mealType: { flex: 1, marginLeft: 10, color: COLORS.text, fontSize: 14, fontWeight: "800" },
  plannedMeal: { flexDirection: "row", alignItems: "center" },
  plannedMealLink: { flex: 1, flexDirection: "row", alignItems: "center" },
  plannedMealImage: { width: 62, height: 62, borderRadius: 13, backgroundColor: COLORS.border },
  plannedMealCopy: { flex: 1, marginLeft: 11 },
  plannedMealTitle: { color: COLORS.text, fontSize: 14, lineHeight: 19, fontWeight: "700" },
  plannedMealCategory: { marginTop: 4, color: COLORS.primary, fontSize: 11, fontWeight: "600" },
  removeButton: {
    width: 36,
    height: 36,
    borderRadius: 12,
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: COLORS.dangerSoft,
  },
  emptySlot: { flexDirection: "row", alignItems: "center", paddingVertical: 7 },
  emptySlotText: { flex: 1, marginLeft: 9, color: COLORS.textLight, fontSize: 13 },
  browseText: { color: COLORS.primary, fontSize: 12, fontWeight: "700" },
  shoppingInputLabelRow: { marginTop: 20, marginBottom: 10, flexDirection: "row", alignItems: "center", justifyContent: "space-between" },
  shoppingInputLabel: { color: COLORS.text, fontSize: 14, fontWeight: "700" },
  listCard: {
    marginTop: 14,
    paddingHorizontal: 14,
    borderRadius: 18,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  shoppingRow: { minHeight: 64, flexDirection: "row", alignItems: "center", borderBottomWidth: 1, borderBottomColor: COLORS.border },
  shoppingRowLast: { borderBottomWidth: 0 },
  checkBox: {
    width: 24,
    height: 24,
    borderRadius: 8,
    alignItems: "center",
    justifyContent: "center",
    borderWidth: 1,
    borderColor: COLORS.border,
    backgroundColor: COLORS.white,
  },
  checkBoxActive: { borderColor: COLORS.primary, backgroundColor: COLORS.primary },
  shoppingItemCopy: { flex: 1, marginLeft: 11 },
  shoppingText: { color: COLORS.text, fontSize: 14, fontWeight: "600" },
  shoppingTextDone: { color: COLORS.textLight, textDecorationLine: "line-through" },
  shoppingSource: { marginTop: 3, color: COLORS.textLight, fontSize: 10 },
  shoppingRemoveButton: { padding: 7 },
});
