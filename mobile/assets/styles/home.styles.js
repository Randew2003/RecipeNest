import { Dimensions, StyleSheet } from "react-native";
import { COLORS } from "../../constants/colors";

const { width } = Dimensions.get("window");
const cardWidth = (width - 48) / 2;

export const homeStyles = StyleSheet.create({
  container: { flex: 1, backgroundColor: COLORS.background },
  scrollContent: { paddingBottom: 30 },
  headerSection: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    paddingHorizontal: 20,
    paddingTop: 16,
    paddingBottom: 14,
  },
  headerCopy: { flex: 1, minWidth: 0, paddingRight: 14 },
  brandRow: { flexDirection: "row", alignItems: "center", marginBottom: 7 },
  brandMark: {
    width: 22,
    height: 22,
    alignItems: "center",
    justifyContent: "center",
    marginRight: 7,
    borderRadius: 8,
    backgroundColor: COLORS.primary,
  },
  brandName: {
    color: COLORS.primary,
    fontSize: 10,
    fontWeight: "800",
    letterSpacing: 1,
  },
  eyebrow: { color: COLORS.textLight, fontSize: 12.5, lineHeight: 17, fontWeight: "600" },
  welcomeText: {
    marginTop: 2,
    color: COLORS.text,
    fontSize: 24,
    lineHeight: 29,
    fontWeight: "800",
    letterSpacing: -0.45,
  },
  avatarButton: {
    width: 46,
    height: 46,
    padding: 3,
    borderRadius: 16,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
    shadowColor: COLORS.shadow,
    shadowOffset: { width: 0, height: 3 },
    shadowOpacity: 0.07,
    shadowRadius: 7,
    elevation: 2,
  },
  headerAvatar: { width: "100%", height: "100%", borderRadius: 13 },
  searchBar: {
    minHeight: 54,
    flexDirection: "row",
    alignItems: "center",
    marginHorizontal: 20,
    marginBottom: 21,
    paddingHorizontal: 16,
    borderRadius: 17,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  searchBarText: { flex: 1, marginLeft: 10, color: COLORS.textLight, fontSize: 14 },
  sectionHeadingRow: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    marginBottom: 13,
  },
  sectionLabel: { color: COLORS.text, fontSize: 19, fontWeight: "800" },
  featuredSection: { paddingHorizontal: 20, marginBottom: 25 },
  featuredCard: {
    height: 235,
    overflow: "hidden",
    borderRadius: 22,
    backgroundColor: COLORS.card,
    shadowColor: COLORS.shadow,
    shadowOffset: { width: 0, height: 8 },
    shadowOpacity: 0.12,
    shadowRadius: 13,
    elevation: 5,
  },
  featuredImage: { width: "100%", height: "100%", backgroundColor: COLORS.border },
  featuredOverlay: { ...StyleSheet.absoluteFillObject, justifyContent: "space-between", padding: 18 },
  featuredBadge: {
    alignSelf: "flex-start",
    paddingHorizontal: 11,
    paddingVertical: 6,
    borderRadius: 15,
    backgroundColor: COLORS.primary,
  },
  featuredBadgeText: { color: COLORS.white, fontSize: 11, fontWeight: "700" },
  featuredTitle: {
    maxWidth: "94%",
    marginBottom: 10,
    color: COLORS.white,
    fontSize: 24,
    lineHeight: 29,
    fontWeight: "800",
  },
  featuredMeta: { flexDirection: "row", flexWrap: "wrap" },
  metaItem: { flexDirection: "row", alignItems: "center", marginRight: 16 },
  metaText: { marginLeft: 5, color: COLORS.white, fontSize: 13, fontWeight: "600" },
  categoryHeader: { paddingHorizontal: 20 },
  categoryFilterContainer: { marginTop: 12, marginBottom: 24 },
  categoryFilterScrollContent: { paddingHorizontal: 20, paddingVertical: 2 },
  categoryButton: {
    minWidth: 82,
    alignItems: "center",
    marginRight: 10,
    paddingHorizontal: 10,
    paddingVertical: 10,
    borderRadius: 17,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  selectedCategory: { backgroundColor: COLORS.primary, borderColor: COLORS.primary },
  categoryImage: {
    width: 42,
    height: 42,
    marginBottom: 7,
    borderRadius: 21,
    backgroundColor: COLORS.border,
  },
  categorySelectedIcon: {
    position: "absolute",
    right: -2,
    bottom: 5,
    width: 18,
    height: 18,
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 9,
    backgroundColor: COLORS.primaryDark,
    borderWidth: 2,
    borderColor: COLORS.white,
  },
  categoryText: { color: COLORS.text, fontSize: 11, fontWeight: "600", textAlign: "center" },
  selectedCategoryText: { color: COLORS.white },
  recipesSection: { paddingHorizontal: 16 },
  sectionTitle: { color: COLORS.text, fontSize: 22, fontWeight: "800", letterSpacing: -0.3 },
  recipesGrid: { flexDirection: "row", flexWrap: "wrap", justifyContent: "space-between" },
  inlineError: {
    marginBottom: 14,
    padding: 12,
    borderRadius: 12,
    color: COLORS.danger,
    backgroundColor: COLORS.dangerSoft,
    fontSize: 13,
    lineHeight: 19,
  },
  emptyState: { alignItems: "center", paddingVertical: 54, paddingHorizontal: 28 },
  fullErrorState: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    paddingHorizontal: 30,
    backgroundColor: COLORS.background,
  },
  errorIcon: {
    width: 68,
    height: 68,
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 22,
    backgroundColor: COLORS.primarySoft,
  },
  emptyTitle: {
    marginTop: 14,
    marginBottom: 7,
    color: COLORS.text,
    fontSize: 20,
    fontWeight: "800",
    textAlign: "center",
  },
  emptyDescription: { color: COLORS.textLight, fontSize: 14, lineHeight: 20, textAlign: "center" },
  retryButton: {
    flexDirection: "row",
    alignItems: "center",
    marginTop: 20,
    paddingHorizontal: 20,
    paddingVertical: 12,
    borderRadius: 15,
    backgroundColor: COLORS.primary,
  },
  retryButtonText: { marginLeft: 8, color: COLORS.white, fontSize: 14, fontWeight: "700" },
});

export const recipeCardStyles = StyleSheet.create({
  container: {
    width: cardWidth,
    overflow: "hidden",
    marginBottom: 16,
    borderRadius: 17,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
    shadowColor: COLORS.shadow,
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.07,
    shadowRadius: 8,
    elevation: 3,
  },
  image: { width: "100%", height: 138, backgroundColor: COLORS.border },
  content: { minHeight: 87, padding: 12 },
  title: { minHeight: 40, color: COLORS.text, fontSize: 15, lineHeight: 20, fontWeight: "700" },
  footer: { flexDirection: "row", alignItems: "center", marginTop: 9 },
  metaItem: { flex: 1, flexDirection: "row", alignItems: "center", paddingRight: 5 },
  metaText: { flex: 1, marginLeft: 4, color: COLORS.textLight, fontSize: 10, fontWeight: "600" },
});
