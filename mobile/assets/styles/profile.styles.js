import { StyleSheet } from "react-native";
import { COLORS } from "../../constants/colors";

export const profileStyles = StyleSheet.create({
  container: { flex: 1, backgroundColor: COLORS.background },
  content: { paddingHorizontal: 20, paddingBottom: 40 },
  headerFlush: { paddingHorizontal: 0, paddingTop: 12 },

  // Identity card (replaces the old full-bleed gradient hero)
  identityCard: {
    flexDirection: "row",
    alignItems: "center",
    marginTop: 14,
    padding: 16,
    borderRadius: 22,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  identityAvatarWrap: {
    width: 64,
    height: 64,
    padding: 3,
    borderRadius: 21,
    backgroundColor: COLORS.primarySoft,
  },
  identityAvatar: { width: "100%", height: "100%", borderRadius: 18, backgroundColor: COLORS.primaryFaint },
  identityCopy: { flex: 1, marginLeft: 14, paddingRight: 6 },
  identityName: { color: COLORS.text, fontSize: 17, fontWeight: "800" },
  identityEmail: { marginTop: 2, color: COLORS.textLight, fontSize: 11.5 },
  identityBadge: {
    alignSelf: "flex-start",
    flexDirection: "row",
    alignItems: "center",
    marginTop: 8,
    paddingHorizontal: 9,
    paddingVertical: 5,
    borderRadius: 10,
    backgroundColor: COLORS.primarySoft,
  },
  identityBadgeText: { marginLeft: 5, color: COLORS.primary, fontSize: 10, fontWeight: "700" },

  // Segmented control
  segmentRow: {
    flexDirection: "row",
    marginTop: 18,
    padding: 4,
    borderRadius: 15,
    backgroundColor: COLORS.primaryFaint,
  },
  segmentItem: {
    flex: 1,
    alignItems: "center",
    paddingVertical: 9,
    borderRadius: 12,
  },
  segmentItemActive: {
    backgroundColor: COLORS.card,
    shadowColor: COLORS.shadow,
    shadowOffset: { width: 0, height: 3 },
    shadowOpacity: 0.08,
    shadowRadius: 6,
    elevation: 2,
  },
  segmentText: { fontSize: 12, fontWeight: "700", color: COLORS.textLight },
  segmentTextActive: { color: COLORS.primary },

  section: { marginTop: 20 },
  sectionHeader: {
    marginBottom: 12,
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
  },
  sectionTitle: { color: COLORS.text, fontSize: 16, fontWeight: "800" },
  sectionHint: { color: COLORS.textLight, fontSize: 11, fontWeight: "600" },

  // Overview tab
  statsCard: {
    flexDirection: "row",
    borderRadius: 18,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  statCell: { flex: 1, alignItems: "center", paddingVertical: 16 },
  statDivider: { width: 1, backgroundColor: COLORS.border },
  statValue: { marginTop: 5, color: COLORS.text, fontSize: 18, fontWeight: "800" },
  statLabel: { marginTop: 2, color: COLORS.textLight, fontSize: 10.5, fontWeight: "600" },

  factRow: {
    flexDirection: "row",
    alignItems: "center",
    marginTop: 10,
    padding: 13,
    borderRadius: 16,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  factIcon: {
    width: 34,
    height: 34,
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 11,
    backgroundColor: COLORS.primarySoft,
  },
  factCopy: { flex: 1, marginLeft: 11 },
  factLabel: { color: COLORS.textLight, fontSize: 10.5, fontWeight: "600" },
  factValue: { marginTop: 1, color: COLORS.text, fontSize: 13.5, fontWeight: "700" },
  factLink: { color: COLORS.primary, fontSize: 11, fontWeight: "700" },

  accountCard: {
    flexDirection: "row",
    alignItems: "center",
    marginTop: 10,
    padding: 15,
    borderRadius: 18,
    backgroundColor: COLORS.dangerSoft,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  accountCopy: { flex: 1, paddingRight: 10 },
  accountTitle: { color: COLORS.text, fontSize: 14.5, fontWeight: "800" },
  accountText: { marginTop: 3, color: COLORS.textLight, fontSize: 11, lineHeight: 16 },
  logoutButton: {
    flexDirection: "row",
    alignItems: "center",
    paddingHorizontal: 12,
    paddingVertical: 10,
    borderRadius: 13,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.danger,
  },
  logoutText: { marginLeft: 6, color: COLORS.danger, fontSize: 12, fontWeight: "700" },

  // Preferences tab
  optionGroup: {
    marginBottom: 12,
    padding: 14,
    borderRadius: 18,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  optionLabel: { marginBottom: 10, color: COLORS.text, fontSize: 13, fontWeight: "700" },
  optionRow: { flexDirection: "row", flexWrap: "wrap", marginHorizontal: -4 },
  option: {
    flexDirection: "row",
    alignItems: "center",
    margin: 4,
    paddingHorizontal: 11,
    paddingVertical: 8,
    borderRadius: 13,
    backgroundColor: COLORS.primaryFaint,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  optionActive: { backgroundColor: COLORS.primarySoft, borderColor: COLORS.primary },
  optionText: { color: COLORS.textLight, fontSize: 11, fontWeight: "600" },
  optionTextActive: { marginLeft: 4, color: COLORS.primary },

  // Journey tab
  progressOverview: {
    marginBottom: 11,
    padding: 14,
    borderRadius: 18,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  progressOverviewTop: { flexDirection: "row", alignItems: "center" },
  progressOverviewIcon: {
    width: 38,
    height: 38,
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 13,
    backgroundColor: COLORS.primarySoft,
  },
  progressOverviewCopy: { flex: 1, marginLeft: 11, paddingRight: 8 },
  progressTitle: { color: COLORS.text, fontSize: 14, fontWeight: "800" },
  progressPercent: { color: COLORS.primary, fontSize: 20, fontWeight: "800" },
  overallProgressTrack: {
    height: 7,
    overflow: "hidden",
    marginTop: 12,
    borderRadius: 6,
    backgroundColor: COLORS.primarySoft,
  },
  overallProgressFill: { height: "100%", borderRadius: 6, backgroundColor: COLORS.primary },
  nextGoalText: {
    marginTop: 3,
    color: COLORS.textLight,
    fontSize: 10.5,
    lineHeight: 14,
    fontWeight: "600",
  },

  journeyCard: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 10,
    padding: 13,
    borderRadius: 17,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  journeyCardComplete: { borderColor: COLORS.success },
  journeyIcon: {
    width: 40,
    height: 40,
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 13,
    backgroundColor: COLORS.primarySoft,
  },
  journeyIconComplete: { backgroundColor: COLORS.success },
  journeyCopy: { flex: 1, marginLeft: 11 },
  journeyTitleRow: { flexDirection: "row", alignItems: "center", justifyContent: "space-between" },
  journeyTitle: { flex: 1, paddingRight: 8, color: COLORS.text, fontSize: 13.5, fontWeight: "800" },
  journeyValue: { color: COLORS.primary, fontSize: 11, fontWeight: "800" },
  journeyValueComplete: { color: COLORS.success },
  journeyDescription: { marginTop: 3, color: COLORS.textLight, fontSize: 10.5, lineHeight: 14.5 },
  journeyProgressTrack: {
    height: 5,
    overflow: "hidden",
    marginTop: 8,
    borderRadius: 5,
    backgroundColor: COLORS.border,
  },
  journeyProgressFill: { height: "100%", borderRadius: 5, backgroundColor: COLORS.primary },
  journeyProgressFillComplete: { backgroundColor: COLORS.success },
});
