import { StyleSheet } from "react-native";
import { COLORS } from "../../constants/colors";

export const recipeDetailStyles = StyleSheet.create({
  container: { flex: 1, backgroundColor: COLORS.background },
  headerContainer: { height: 320, position: "relative", backgroundColor: COLORS.border },
  headerImage: { width: "100%", height: "100%" },
  gradientOverlay: { ...StyleSheet.absoluteFillObject },
  floatingButtons: {
    position: "absolute",
    top: 14,
    left: 16,
    right: 16,
    flexDirection: "row",
    justifyContent: "space-between",
  },
  floatingButton: {
    width: 42,
    height: 42,
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 14,
    backgroundColor: "rgba(25,17,29,0.64)",
    borderWidth: 1,
    borderColor: "rgba(255,255,255,0.22)",
  },
  savedFloatingButton: { backgroundColor: COLORS.primary, borderColor: COLORS.primary },
  disabledButton: { opacity: 0.58 },
  titleSection: { position: "absolute", left: 18, right: 18, bottom: 21 },
  categoryBadge: {
    alignSelf: "flex-start",
    marginBottom: 9,
    paddingHorizontal: 10,
    paddingVertical: 5,
    borderRadius: 13,
    backgroundColor: COLORS.primary,
  },
  categoryText: { color: COLORS.white, fontSize: 10, fontWeight: "700", textTransform: "uppercase" },
  recipeTitle: { color: COLORS.white, fontSize: 26, lineHeight: 31, fontWeight: "800", letterSpacing: -0.5 },
  locationRow: { flexDirection: "row", alignItems: "center", marginTop: 8 },
  locationText: { marginLeft: 5, color: COLORS.white, fontSize: 13, fontWeight: "600" },
  contentSection: { paddingHorizontal: 16, paddingTop: 17, paddingBottom: 38 },
  statsContainer: { flexDirection: "row", marginHorizontal: -5, marginBottom: 14 },
  statCard: {
    flex: 1,
    flexDirection: "row",
    alignItems: "center",
    marginHorizontal: 5,
    padding: 14,
    borderRadius: 16,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  statCopy: { marginLeft: 9 },
  statValue: { color: COLORS.text, fontSize: 14, fontWeight: "800" },
  statLabel: { marginTop: 2, color: COLORS.textLight, fontSize: 10, fontWeight: "600" },
  actionsRow: { flexDirection: "row", marginHorizontal: -5, marginBottom: 18 },
  primaryAction: {
    flex: 1,
    minHeight: 50,
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    marginHorizontal: 5,
    borderRadius: 16,
    backgroundColor: COLORS.primary,
  },
  primaryActionText: { marginLeft: 7, color: COLORS.white, fontSize: 13, fontWeight: "700" },
  secondaryAction: {
    flex: 1,
    minHeight: 50,
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    marginHorizontal: 5,
    borderRadius: 16,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.primary,
  },
  secondaryActionText: { marginLeft: 7, color: COLORS.primary, fontSize: 13, fontWeight: "700" },
  linkCard: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 25,
    padding: 13,
    borderRadius: 16,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  linkIcon: {
    width: 38,
    height: 38,
    alignItems: "center",
    justifyContent: "center",
    marginRight: 10,
    borderRadius: 13,
    backgroundColor: COLORS.dangerSoft,
  },
  linkCardDisabled: { opacity: 0.62 },
  linkCopy: { flex: 1, minWidth: 0 },
  linkText: { color: COLORS.text, fontSize: 13, fontWeight: "800" },
  linkSubtext: { marginTop: 2, color: COLORS.textLight, fontSize: 10.5, lineHeight: 14 },
  sectionTitleRow: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    marginTop: 5,
    marginBottom: 13,
  },
  sectionTitle: { color: COLORS.text, fontSize: 21, fontWeight: "800" },
  countText: { color: COLORS.textLight, fontSize: 12, fontWeight: "600" },
  ingredientsGrid: { marginBottom: 25 },
  ingredientCard: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 9,
    padding: 13,
    borderRadius: 14,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  ingredientText: { flex: 1, marginLeft: 10, color: COLORS.text, fontSize: 14, lineHeight: 20, fontWeight: "600" },
  instructionsContainer: { marginBottom: 15 },
  instructionCard: {
    flexDirection: "row",
    alignItems: "flex-start",
    marginBottom: 11,
    padding: 15,
    borderRadius: 16,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  stepIndicator: {
    width: 32,
    height: 32,
    alignItems: "center",
    justifyContent: "center",
    marginRight: 12,
    borderRadius: 11,
    backgroundColor: COLORS.primarySoft,
  },
  stepNumber: { color: COLORS.primary, fontSize: 13, fontWeight: "800" },
  instructionText: { flex: 1, color: COLORS.text, fontSize: 14, lineHeight: 21 },
  missingContent: { marginBottom: 24, color: COLORS.textLight, fontSize: 13 },
  sourceButton: {
    minHeight: 48,
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 15,
    backgroundColor: COLORS.primarySoft,
  },
  sourceButtonText: { marginLeft: 7, color: COLORS.primary, fontSize: 13, fontWeight: "700" },
  errorContainer: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    paddingHorizontal: 28,
    backgroundColor: COLORS.background,
  },
  errorIcon: {
    width: 68,
    height: 68,
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 22,
    backgroundColor: COLORS.primarySoft,
  },
  errorTitle: { marginTop: 15, color: COLORS.text, fontSize: 21, fontWeight: "800" },
  errorDescription: { marginTop: 7, color: COLORS.textLight, fontSize: 14, lineHeight: 20, textAlign: "center" },
  errorActions: { flexDirection: "row", marginTop: 20 },
  secondaryErrorButton: {
    minHeight: 46,
    justifyContent: "center",
    marginRight: 9,
    paddingHorizontal: 18,
    borderRadius: 14,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  secondaryErrorButtonText: { color: COLORS.text, fontSize: 13, fontWeight: "700" },
  errorButton: {
    minHeight: 46,
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    paddingHorizontal: 18,
    borderRadius: 14,
    backgroundColor: COLORS.primary,
  },
  errorButtonText: { marginLeft: 7, color: COLORS.white, fontSize: 13, fontWeight: "700" },
  modalBackdrop: { flex: 1, justifyContent: "flex-end", backgroundColor: "rgba(28,18,32,0.42)" },
  modalCard: {
    paddingHorizontal: 18,
    paddingTop: 18,
    paddingBottom: 28,
    borderTopLeftRadius: 25,
    borderTopRightRadius: 25,
    backgroundColor: COLORS.background,
  },
  modalHeader: { flexDirection: "row", alignItems: "center", justifyContent: "space-between", marginBottom: 20 },
  modalTitleCopy: { flex: 1, paddingRight: 12 },
  modalTitle: { color: COLORS.text, fontSize: 21, fontWeight: "800" },
  modalRecipeTitle: { marginTop: 4, color: COLORS.textLight, fontSize: 12 },
  modalClose: {
    width: 40,
    height: 40,
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 14,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  modalLabel: { marginTop: 5, marginBottom: 10, color: COLORS.text, fontSize: 13, fontWeight: "700" },
  modalChips: { paddingBottom: 18 },
  modalChip: {
    marginRight: 8,
    paddingHorizontal: 14,
    paddingVertical: 9,
    borderRadius: 14,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  modalChipSelected: { backgroundColor: COLORS.primary, borderColor: COLORS.primary },
  modalChipText: { color: COLORS.textLight, fontSize: 12, fontWeight: "700" },
  modalChipTextSelected: { color: COLORS.white },
  modalMealRow: { flexDirection: "row", marginHorizontal: -4 },
  modalMeal: {
    flex: 1,
    alignItems: "center",
    marginHorizontal: 4,
    paddingVertical: 11,
    borderRadius: 14,
    backgroundColor: COLORS.card,
    borderWidth: 1,
    borderColor: COLORS.border,
  },
  modalMealSelected: { backgroundColor: COLORS.primary, borderColor: COLORS.primary },
  modalMealText: { color: COLORS.text, fontSize: 11, fontWeight: "700" },
  modalMealTextSelected: { color: COLORS.white },
  modalButton: {
    minHeight: 52,
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    marginTop: 22,
    borderRadius: 16,
    backgroundColor: COLORS.primary,
  },
  modalButtonText: { marginLeft: 8, color: COLORS.white, fontSize: 14, fontWeight: "700" },
});
